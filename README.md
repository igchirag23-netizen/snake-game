# Snake

Minimal classic Snake built with plain HTML, CSS, and JavaScript.

## Run

From the repo root:

```bash
python3 -m http.server 8000
```

Then open:

- `http://localhost:8000/` for the game
- `http://localhost:8000/tests/game.test.html` for the browser-based logic checks
